[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: HTTP tests with SQLite and mocked AI
filterwarnings =
    ignore::DeprecationWarning
